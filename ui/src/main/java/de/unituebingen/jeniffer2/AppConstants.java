package de.unituebingen.jeniffer2;

import java.awt.*;

public final class AppConstants {

    public static final int SCREEN_WIDTH = Toolkit.getDefaultToolkit().getScreenSize().width;
    public static final int SCREEN_HEIGHT = Toolkit.getDefaultToolkit().getScreenSize().height;
    public static final int SCREEN_WIDTH_15P = (SCREEN_WIDTH / 100) * 15;
    public static final int SCREEN_WIDTH_20P = (SCREEN_WIDTH / 100) * 20;
    public static final int SCREEN_WIDTH_25P = SCREEN_WIDTH / 4;
    public static final int SCREEN_WIDTH_50P = SCREEN_WIDTH / 2;
    public static final int SCREEN_WIDTH_60P = (SCREEN_WIDTH / 100) * 60;
}
